{
  "name": "shopify-utilities",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "wsrun": "^2.0.0"
  },
  "scripts": {
    "build": "wsrun build --stages",
    "lint": "wsrun lint --fast-exit"
  }
}
